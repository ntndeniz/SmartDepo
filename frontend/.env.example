# Bu dosyayı .env olarak kopyalayıp kendi değerlerinizi girin (yerel geliştirmede opsiyoneldir,
# girilmezse http://localhost:5200/api kullanılır). Netlify'de bu değişkeni proje ayarlarından
# (Site settings → Environment variables) tanımlayın — backend'inizin gerçek, herkese açık URL'i.
VITE_API_URL=http://localhost:5200/api
