[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Tek sayfa (SPA) uygulaması: doğrudan alt route'lara girilse bile index.html'e düş.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
